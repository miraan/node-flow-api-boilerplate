// @flow

export type Produce = {
  id: number,
  name: string,
  quantity: number,
  price: number,
}

export type User = {
  id: number,
  firstName: string,
  lastName: string,
  facebookId: string,
  facebookAccessToken: string,
  level: number,
}
